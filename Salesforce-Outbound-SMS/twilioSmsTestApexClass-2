@isTest
public with sharing class twilioSmsApexClassTest {
       
    static testMethod void test_sendSmsApexRequest() {
    
        // Create a test Contact
        Contact c = new Contact();
        c.LastName = 'Test';
        c.Phone = 'XXXXXXXXXXX';
        c.MobilePhone = 'XXXXXXXXXXX';
        insert c;
        
        // Set the mock callout class
        Test.setMock(HttpCalloutMock.class, new MockHttpResponseGenerator());
        
        // Starting the Test Context
        Test.startTest();
        
        // Call the method to test
        Map <String, Object> res = twilioSmsApexClass.sendSmsApexRequest(c.Id, 'Test Message');
        
        // Assert that res contains values expected from Http Callout
        System.assertEquals(res.get('foo'), 'bar'); // Adjust according to your expected results
        System.assertEquals(res.get('success'), true); // Adjust according to your expected results
        System.assertEquals(res.size(), 2); // Adjust according to your expected results

        // Ending the Test Context
        Test.stopTest();
    }
}